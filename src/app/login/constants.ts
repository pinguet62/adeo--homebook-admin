/**
 * Before redirect to login page,
 * save the original URL,
 * in order to redirect user to initial target page.
 */
export const ORIGINAL_URL = 'originalUrl';

export const LOGIN_ROUTE = 'login';
